cover:
  service:
    cover-types: "Washington DC, Atlanta, Reno, Panama City"
    random-delay: false

endpoints:
  mappings:
    sensitive: false
  beans:
    sensitive: false
  configprops:
    sensitive: false
  env:
    sensitive: false

management:
  security:
    enabled: false

logging:
  level:
    io:
      pivotalservices: DEBUG
